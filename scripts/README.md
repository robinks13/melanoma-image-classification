# Command-line workflows

`train.py` trains and saves the best validation checkpoint. `predict.py` loads a checkpoint and predicts one image. From the repository root, install the package with `pip install -r requirements.txt`, then run either script with `python scripts/<name>.py --help` for options.
